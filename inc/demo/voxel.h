#ifndef VOXEL_HPP
#define VOXEL_HPP

#include <glad/gl.h>
/*#include "noise.hpp"

#include <cstddef>
#include <cstdint>

// Greedy meshing implementation

struct Voxel {
    bool enabled = false;
};

constexpr size_t CHUNK_WIDTH = 16ULL;

struct ChunkMesh {
    GLuint vao;
    GLuint vbo;
    GLuint ebo;
};

**
 * 3D array of `Voxel`s
 *
struct Chunk {
    Voxel voxels[CHUNK_WIDTH][CHUNK_WIDTH][CHUNK_WIDTH]; // basic C-style array used since voxels are PODs

    ChunkMesh mesh = {};

    **
     * @brief Generates a new mesh from this chunk. Uses dumb meshing.
     *
    auto GenerateNewMesh_Worst() -> const ChunkMesh&;
};*/

#endif
