#version 330 core 
out vec4 FragColor;

void main() {
    // cyan color  
    // FragColor = vec4(0.5f, 0.0f, 0.5f, 1.0f);
    // funne editor color it looks kinda like blender 
    // FragColor = vec4(0.3f, 0.3f, 0.35f, 1.0f);
    // the fucking dark void 
    FragColor = vec4(0.01f, 0.01f, 0.01f, 1.0f);
}

